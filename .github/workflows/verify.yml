name: Verify Email Step

on:
  push:
    branches: [ main ]
  workflow_dispatch:   # allows manual run from Actions UI

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: 22f1000684@ds.study.iitm.ac.in
        run: |
          echo "This step's name contains the requested email address"
          echo "Hello, world!"
